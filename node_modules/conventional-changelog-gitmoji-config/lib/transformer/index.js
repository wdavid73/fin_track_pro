"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _syncFetch = _interopRequireDefault(require("@ardatan/sync-fetch"));
var _commitTypes = _interopRequireDefault(require("@gitmoji/commit-types"));
var _pangu = _interopRequireDefault(require("pangu"));
var _scopeMapDisplayName = require("./scopeMapDisplayName");
var _typeDisplayName = require("./typeDisplayName");
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
var capitalizeFirstLetter = function capitalizeFirstLetter(str) {
  var firstLetter = String(str).slice(0, 1).toUpperCase();
  var remainingStr = String(str).slice(1);
  return firstLetter + remainingStr;
};
var cacheUsers = {};
var _default = function _default(customConfig) {
  return function (commit, context) {
    var discard = true;
    var issues = [];
    commit.notes.forEach(function (note) {
      note.title = "".concat((customConfig === null || customConfig === void 0 ? void 0 : customConfig.withEmoji) === false ? '' : '💥 ', "BREAKING CHANGES");
      discard = false;
    });
    var displayTypes = _commitTypes.default;
    if (customConfig.displayTypes) {
      displayTypes = customConfig.displayTypes;
    }
    if (!displayTypes.includes(commit.type) && discard) return;

    // 修改 type 标题
    commit.type = (0, _typeDisplayName.getDisplayName)(commit.type, {
      language: customConfig.titleLanguage,
      withEmoji: customConfig.withEmoji,
      customTypeMap: customConfig.customTypeMap
    });

    /** * 处理 scope ** */
    if (commit.scope === '*') {
      commit.scope = '';
    }
    if (customConfig.displayScopes) {
      var _customConfig$display;
      if (!((_customConfig$display = customConfig.displayScopes) !== null && _customConfig$display !== void 0 && _customConfig$display.includes(commit.scope))) return;
    }
    if (customConfig.scopeDisplayName) {
      commit.scope = (0, _scopeMapDisplayName.scopeMapDisplayName)(commit.scope, customConfig.scopeDisplayName);
    }
    if (typeof commit.hash === 'string') {
      commit.hash = commit.hash.substring(0, 7);
    }
    if (typeof commit.subject === 'string') {
      var url = context.repository ? "".concat(context.host, "/").concat(context.owner, "/").concat(context.repository) : context.repoUrl;
      if (url) {
        url = "".concat(url, "/issues/");
        // Issue URLs.
        commit.subject = commit.subject.replace(/#([0-9]+)/g, function (_, issue) {
          issues.push(issue);
          return "[#".concat(issue, "](").concat(url).concat(issue, ")");
        });
      }
      if (context.host) {
        // User URLs.
        commit.subject = commit.subject.replace(/\B@([a-z0-9](?:-?[a-z0-9/]){0,38})/g, function (_, username) {
          if (username.includes('/')) {
            return "@".concat(username);
          }
          return "[@".concat(username, "](").concat(context.host, "/").concat(username, ")");
        });
      }
    }

    // remove references that already appear in the subject
    commit.references = commit.references.filter(function (reference) {
      return issues.indexOf(reference.issue) === -1;
    });

    // format
    if (commit.subject) {
      commit.rawSubject = commit.subject;
      commit.subject = _pangu.default.spacing(capitalizeFirstLetter(commit.subject));
    }
    if (customConfig.showAuthor && customConfig.showAuthorAvatar && commit.authorEmail) {
      if (!cacheUsers[commit.authorEmail]) {
        var _authorInfo$items;
        var authorInfo = (0, _syncFetch.default)("https://api.github.com/search/users?q=".concat(commit.authorEmail)).json();
        if ((authorInfo === null || authorInfo === void 0 ? void 0 : (_authorInfo$items = authorInfo.items) === null || _authorInfo$items === void 0 ? void 0 : _authorInfo$items.length) > 0) {
          cacheUsers[commit.authorEmail] = authorInfo.items[0];
        } else {
          cacheUsers[commit.authorEmail] = 'unknown';
        }
      }
      if (cacheUsers[commit.authorEmail] !== 'unknown') {
        var userInfo = cacheUsers[commit.authorEmail];
        commit.authorName = userInfo.login;
        commit.authorAvatar = userInfo.avatar_url;
        commit.authorUrl = userInfo.url;
      }
    }
    return commit;
  };
};
exports.default = _default;
module.exports = exports.default;