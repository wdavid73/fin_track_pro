"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _parserOpts = _interopRequireDefault(require("./parserOpts"));
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
/* istanbul ignore file */
var _default = {
  parserOpts: _parserOpts.default,
  whatBump: function whatBump(commits) {
    var level = 2;
    var breakings = 0;
    var features = 0;
    commits.forEach(function (commit) {
      if (commit.notes.length > 0) {
        breakings += commit.notes.length;
        level = 0;
      } else if (commit.type === "feat") {
        features += 1;
        if (level === 2) {
          level = 1;
        }
      }
    });
    return {
      level: level,
      reason: breakings === 1 ? "There is ".concat(breakings, " BREAKING CHANGE and ").concat(features, " features") : "There are ".concat(breakings, " BREAKING CHANGES and ").concat(features, " features")
    };
  }
};
exports.default = _default;
module.exports = exports.default;